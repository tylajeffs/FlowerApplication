<% title "Login" %>

<%= bootstrap_form_with(url: sessions_path, method: :post) do |f| %>
    <%= f.email_field :email %>
    <%= f.password_field :password %>
    <%= f.primary 'Log in ' %>
<% end %>
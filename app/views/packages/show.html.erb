<br><br><br>

<h1>Package</h1>

<%= render @package %>

<%= link_to "All Packages", packages_path %> |
<%= link_to "Edit Package", edit_package_path(@package) %> |
<%= link_to "View Reviews", package_reviews_path(@package) %>

<%= button_to 'Delete Package', @package, method: :delete , class: 'btn btn_primary', form: { data: {turbo_confirm: "Are you sure?"} } %>